#include <stdio.h>

main()
{

    int length = 10;
    int width = 20;
    int area = length * width;

    printf(" area of rectangle %d * %d = %d\n", length, width, area);

    return 0;
}